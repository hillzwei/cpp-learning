// Standard library uses <xxx> while user library uses "xxx"
#include<iostream>

// When not use namespace std, cout will be write as std::cout
using namespace std;

int main() {
    // Output to standard output.
    cout << "hello, it works" << endl;
    return 0;
}
